'use client'
import Header from '@/components/Header'
import React from 'react'

type Props = {
}

export default function Page({}: Props) {
  return (
    <div>
      <Header activeItem={1}/>
    </div>
    
  )
}